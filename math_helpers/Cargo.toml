[package]
name = "math_helpers"
version.workspace = true
edition.workspace = true

[dev-dependencies]
criterion = { version = "0.7.0", features = ["html_reports"] }
rand = "0.9.2"

[[bench]]
name = "scal_bench"
harness = false
